# Project Information
project.name=NopCommerce Automation

# Browser Configuration
# Options: ChromeLocal, FirefoxLocal, EdgeLocal
browser.type=ChromeLocal

# Selenium Grid (not used here since no LambdaTest/SauceLabs license)
selenium.grid.type=local

# Timeouts (in seconds)
selenium.driver.wait.timeout=20

# Screenshot capture (true = enabled, false = disabled)
screenshot.enabled=true
